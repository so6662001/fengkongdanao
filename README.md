# 钢铁行业 AI 风控大脑 · 高保真原型

面向钢贸企业的 AI 风控体系高保真 HTML 原型。在原有「内部库存滞销 + 应收欠款坏账」风控体系基础上，接入自有钢铁资源平台三大核心数据，形成 **「内部经营数据 + 行业全域市场数据」双驱动**风控体系，并保留业务回流、持续迭代的数据飞轮闭环。

## 快速开始

纯静态页面，零构建、零外部依赖（图表为自研 SVG 实现，**离线可直接打开**）。

```bash
# 方式一：直接用浏览器打开
open index.html

# 方式二：本地起一个静态服务（推荐，避免个别浏览器的 file:// 限制）
python3 -m http.server 8080
# 然后访问 http://localhost:8080
```

建议浏览器窗口宽度 ≥ 1440px 以获得最佳信息密度。

## 页面清单

| 页面 | 文件 | 对应方案内容 |
| --- | --- | --- |
| 风控大脑总览 | `index.html` | 经营风险 KPI、双驱动数据打通链路、预警流、工单回流、模型效果 |
| 四层架构全景 | `architecture.html` | 数据接入层 → 模型引擎层 → 业务执行预警层 → 可视化层，含避坑要点 |
| 数据接入层 | `data-source.html` | 14 类数据源清单、500+ 商户报价流、行情指数 API、失信检测 API、规格编码标准化映射工作台、向量知识库 |
| 模型引擎层 | `model-engine.html` | 量化预测模型（呆滞跌价 / 坏账 / 供应链风险子模型）特征扩容、三大 Agent、工具集、RAG→LoRA 选型策略 |
| Agent 推理实录 | `agent-detail.html` | 三个典型推理场景的完整推理链、多套处置方案测算、人工决策回填 |
| 库存风险看板 | `dashboard-inventory.html` | 批次呆滞概率与浮亏、**市场抛压指数面板**（挂牌热度曲线 + 同业价格曲线 vs 成本线） |
| 应收欠款风险看板 | `dashboard-receivable.html` | 客户卡片直接展示失信事件 + 大模型解读 + 平台挂牌抛售行为 |
| 全局经营风控总看板 | `dashboard-global.html` | 三线风险总览、**行业市场风险总览**、品类↔下游客户联动表、自然语言问答 |
| 业务执行与预警 | `workflow.html` | 风险分级推送、采购审批节点风控增强、处置工单闭环、上游供应商风险告警 |
| 数据飞轮与路线图 | `flywheel.html` | 数据飞轮五步闭环、案例素材池、三期落地路线、关键避坑要点 |

全局能力：右上角「AI 问答」抽屉（自然语言一句话提问）、通知铃铛的风险预警中心，在所有页面均可使用。

## 本次升级的核心增量

三大自有平台数据源接入（页面中以 `NEW` / 青色标识）：

1. **500+ 商家每日货源价格数据流** —— 挂牌货源、规格材质、挂牌单价、挂牌余量、上新/撤牌动态，按「规格-区域-时间」时序存储；热门规格小时级同步，长尾规格每日全量快照。
2. **平台行情指数 API** —— 分品类行情指数、区域热度指数、成交情绪指数，作为贴合自身商圈的先行指标优先入模。
3. **企业失信风险检测 API** —— 两层调用：下游赊销客户征信巡检 + 平台入驻商户（上游供货商）风险监控。

由此新增的能力：市场抛压等级与短期行情情绪等级输出、供应链风险预测子模型、采购审批节点的市场供需风控、上游供应商风险告警、行业市场风险总览面板。

## 目录结构

```
.
├── index.html                     # 总览
├── architecture.html              # 四层架构
├── data-source.html               # 第一层：数据接入
├── model-engine.html              # 第二层：模型引擎
├── agent-detail.html              # Agent 推理实录
├── dashboard-inventory.html       # 库存风险看板
├── dashboard-receivable.html      # 应收风险看板
├── dashboard-global.html          # 全局总看板
├── workflow.html                  # 第三层：业务执行与预警
├── flywheel.html                  # 数据飞轮与落地路线
└── assets
    ├── css/main.css               # 设计令牌与全部组件样式
    └── js
        ├── charts.js              # 零依赖 SVG 图表库（折线/柱状/排名/环形/仪表/雷达/热力/流向）
        ├── data.js                # 模拟业务数据（品类、客户、供货商、报价流、工单、规格映射…）
        └── app.js                 # 侧边导航、顶栏、AI 问答抽屉、预警中心、通用组件
```

## 说明

- 页面中所有数值均为**演示用模拟数据**，字段口径按真实钢贸 ERP 与资源平台设计，并做了跨页面交叉一致（如各品类货值合计 = 库存总货值、高风险客户应收合计 = 坏账高风险敞口）。
- 原型强调一个定位：**AI 只做决策辅助，不自动下达业务指令**。所有处置建议都需要人工在工单中填写采纳 / 部分采纳 / 否决与执行动作，结果回填后归档为案例。
